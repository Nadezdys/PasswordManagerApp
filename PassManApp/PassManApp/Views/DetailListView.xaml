<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Title="Detail hesla"
             x:Class="PassManApp.Views.DetailListView">
    <ContentPage.ToolbarItems>
        <ToolbarItem Icon="TrashIcon.png" Text="Odstranit" Clicked="Delete_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Margin="20,0,20,0">
            <Label Text="Název"/>
            <Entry x:Name="nameEntry"
               Placeholder="Název" />
            <Label Text="Uživatelské jméno" />
            <Entry x:Name="usernameEntry"
               Placeholder="Uživatelské jméno" />
            <Label Text="Heslo"/>
            <Entry x:Name="passwordEntry"
               Placeholder="Heslo" />
            <Label Text="Webová stránka" />
            <Entry x:Name="webEntry"
               Placeholder="Webová stránka" />
            <Label Text="Poznámka" />
            <Entry x:Name="noteEntry"
               Placeholder="Poznámka" />
            <Button Text="Uložit"
                Clicked="saveButton_Clicked" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>